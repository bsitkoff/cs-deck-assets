# Attendance question slides — 2026-27

102 projected slides: 34 per grade, one per meeting week, rendered from the
Spark Creativity attendance-question deck at 1400px wide.

`g6-w01-q002.jpg` = Grade 6, week 1, page 2 of the 182-page source deck.

These are the `imageUrl` in a CourseWare `attendance_question` warmup payload.
The image is **board-only** — it is projected, and never reaches a student
payload. Student-visible options stay generic (`A`/`B` or `#1`–`#4`) so that
answering is evidence of being in the room.
